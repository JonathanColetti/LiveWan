# The exact versions this project was built and verified against, on one
# A100-40GB (Linux, Python 3.12, CUDA driver 13.0). `pyproject.toml` carries
# looser lower bounds for normal use; this file is for reproducing the runs
# behind the numbers in the README.
#
#   pip install torch==2.12.0 torchvision==0.27.0 --index-url https://download.pytorch.org/whl/cu130
#   pip install -r requirements-lock.txt
#   pip install -e . --no-deps
#
# torch is listed for the record but must come from the CUDA-specific index above,
# not from PyPI -- the wheel has to match your GPU architecture (README, Minimum spec).

# torch==2.12.0+cu130
# torchvision==0.27.0+cu130

einops==0.8.2
numpy==2.4.6
safetensors==0.8.0
huggingface_hub==1.18.0
transformers==5.15.0
sentencepiece==0.2.2
opencv-python-headless==5.0.0.93
fastapi==0.141.1
uvicorn==0.52.1
websockets==17.0.1
pydantic==2.13.4

# extras: mp4 output (livewan-offline)
imageio==2.37.4
imageio-ffmpeg==0.6.0

# extras: dev/test
requests==2.34.2
